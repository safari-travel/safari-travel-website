import React from 'react';
import ReactDOM from 'react-dom/client';
import SafariWebsite from './safari_website';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SafariWebsite />
  </React.StrictMode>
);
